[[modules]]
= 模块

[partintro]
--
本章涵盖了负责 Elasticsearch 方方面面功能的模块。每个模块的设定可以是：

_静态_::

这些设定必须在节点级别进行设置，或在“elasticsearch.yml”文件中，或作为环境变量或包含在启动节点时的命令行中。他们必须在集群中每一个相关节点中都进行设置。

_动态_::

这些设定可以在一个运行的集群中使用
<<cluster-update-settings,集群更新设定>>API进行动态更新。

本章的模型包括：

<<modules-cluster,集群级别路由和分片分配>>::

    控制分片何时，怎样，到哪儿的节点间的分配的设定。

<<modules-discovery,发现>>::

    节点如何发现彼此从而形成集群。

<<modules-gateway,网关>>::

    需要多少个节点加入集群方能使恢复启动。

<<modules-http,HTTP>>::

    控制 HTTP REST 接口的设置。

<<modules-indices,索引>>::

    全局的索引相关的设置。

<<modules-network,网络>>::

    控制默认的网络设置。

<<modules-node,节点客户端>>::

    一个加入集群但既不保存数据也不作为主节点的 Java 客户端。

<<modules-scripting-painless,Painless>>::

    一个为 Elasticsearch 设计的尽可能安全的内建脚本语言。

<<modules-plugins,插件>>::

    使用插件扩展 Elasticsearch。

<<modules-scripting,脚本>>::

    可在 Lucene 表达式以及 Groovy 中使用的自定义脚本。你也能使用内建的脚本语言来书写脚本，<<modules-scripting-painless, Painless>>。

<<modules-snapshots,快照/复原>>::

    使用 快照/复原 备份你的数据。

<<modules-threadpool,线程池>>::

    关于 Elasticsearch 中专用的线程池

<<modules-transport,传输>>::

    配置 Elasticsearch 内部用于节点间通信的网络传输层。

<<modules-tribe,族节点>>::

    加入一个或多个集群，作为跨集群的联合客户端的族节点。

<<modules-remote-clusters, 远程集群>>::

    远程集群运行在这样的特质上：通过传输层连通各集群


<<modules-cross-cluster-search, 跨集群搜索>>::

    跨集群搜索使得在多个集群执行搜索请求而不需加入这些集群成为可能，并将作为集群间的一个联合客户端。
--


include::modules/cluster.asciidoc[]

include::modules/discovery.asciidoc[]

include::modules/gateway.asciidoc[]

include::modules/http.asciidoc[]

include::modules/indices.asciidoc[]

include::modules/network.asciidoc[]

include::modules/node.asciidoc[]

:edit_url:
include::modules/plugins.asciidoc[]

include::modules/scripting.asciidoc[]

include::modules/snapshots.asciidoc[]

include::modules/threadpool.asciidoc[]

include::modules/transport.asciidoc[]

include::modules/tribe.asciidoc[]

include::modules/remote-clusters.asciidoc[]

include::modules/cross-cluster-search.asciidoc[]
